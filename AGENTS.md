## Agent skills

### Issue tracker

Issues are tracked on GitHub (datamathcode/gitea-mcp), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at repo root, created lazily as needed. See `docs/agents/domain.md`.
